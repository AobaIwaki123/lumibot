apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lumibot

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - secret.yaml

images:
  - name: ghcr.io/aobaiwaki123/lumibot
    newTag: v0.1.0
