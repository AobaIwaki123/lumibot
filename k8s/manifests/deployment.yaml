apiVersion: apps/v1
kind: Deployment
metadata:
  name: lumibot
  labels:
    app: lumibot
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: lumibot
  template:
    metadata:
      labels:
        app: lumibot
    spec:
      securityContext:
        fsGroup: 65532 # nonroot group for distroless
      containers:
        - name: lumibot
          image: ghcr.io/aobaiwaki123/lumibot:latest
          imagePullPolicy: Always
          env:
            - name: LUMITREE_API_URL
              value: "http://lumitree.lumitree.svc.cluster.local:8080"
            - name: LUMIBOT_DB_PATH
              value: "/data/lumibot.db"
            - name: DISCORD_TOKEN
              valueFrom:
                secretKeyRef:
                  name: lumibot-secrets
                  key: discord-token
          ports:
            - containerPort: 8080
              name: health
          livenessProbe:
            httpGet:
              path: /health
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: data-volume
              mountPath: /data
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
      volumes:
        - name: data-volume
          persistentVolumeClaim:
            claimName: lumibot-pvc
